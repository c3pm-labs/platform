# Migration `20200716221150-create-package-model`

This migration has been generated by Gabriel Colson at 7/16/2020, 10:11:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"apiKey" text  NOT NULL ,"email" text  NOT NULL ,"id" text  NOT NULL ,"password" text  NOT NULL ,"username" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Package" (
"authorId" text  NOT NULL ,"name" text  NOT NULL ,
    PRIMARY KEY ("name"))

CREATE TABLE "public"."Version" (
"description" text  NOT NULL ,"license" text  NOT NULL ,"packageName" text  NOT NULL ,"publishedAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"readme" text  NOT NULL ,"version" text  NOT NULL ,
    PRIMARY KEY ("version","packageName"))

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "User.username" ON "public"."User"("username")

CREATE UNIQUE INDEX "User.apiKey" ON "public"."User"("apiKey")

ALTER TABLE "public"."Package" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Version" ADD FOREIGN KEY ("packageName")REFERENCES "public"."Package"("name") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200613181527-add-api-key..20200716221150-create-package-model
--- datamodel.dml
+++ datamodel.dml
@@ -3,14 +3,34 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model User {
-  id       String @default(cuid()) @id
-  email    String @unique
+  id       String    @default(cuid()) @id
+  email    String    @unique
   password String
-  username String @unique
-  apiKey   String @default(cuid()) @unique
+  username String    @unique
+  apiKey   String    @default(cuid()) @unique
+  packages Package[]
 }
+
+model Package {
+  name     String    @id
+  author   User      @relation(fields: [authorId], references: [id])
+  authorId String
+  versions Version[]
+}
+
+model Version {
+  package     Package  @relation(fields: [packageName], references: [name])
+  packageName String
+  version     String
+  readme      String
+  description String
+  license     String
+  publishedAt DateTime @default(now())
+
+  @@id([version, packageName])
+}
```


